// Expo's global types (e.g. `import '@/global.css'`). `npx expo start` also writes this reference
// into the gitignored expo-env.d.ts; keeping it here makes `npm run typecheck` work in a fresh clone and CI.
/// <reference types="expo/types" />
